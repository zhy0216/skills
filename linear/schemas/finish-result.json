{
  "type": "object",
  "additionalProperties": false,
  "required": ["issueId", "outcome", "baseBranch", "commit", "validationCommentId", "summary"],
  "properties": {
    "issueId": { "type": "string" },
    "outcome": { "type": "string", "enum": ["completed", "blocked", "failed"] },
    "baseBranch": { "type": "string", "enum": ["main", "master"] },
    "commit": { "type": ["string", "null"] },
    "validationCommentId": { "type": ["string", "null"] },
    "summary": { "type": "string" }
  }
}
